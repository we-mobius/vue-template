{
  // extend your base config so you don't have to redefine your compilerOptions
  // TODO: extends a predefined base config file
  "extends": "./tsconfig.app.json",
  "include": [
    "env.d.ts",
    "src/**/*.vue",
    "src/**/*.ts",
    "tailwind.config.*",
    "postcss.config.*",
    "vite.config.*",
    "vitest.config.*"
  ]
}
